services:
  whiteboard:
    build: .
    container_name: whiteboard
    ports:
      - "2452:2452"
    volumes:
      # Persist all data including notes, media, and system files (users.json, settings.json)
      # users.json and settings.json are now stored in data/_system/ for Docker volume persistence
      - whiteboard-data:/app/data
      # Persist shared notes metadata (will be created by app if missing)
      - whiteboard-shared:/app/shared
    environment:
      - NODE_ENV=production
      - PORT=2452
      - SESSION_SECRET=${SESSION_SECRET:-change-this-secret-in-production}
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://localhost:2452/login.html"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

volumes:
  whiteboard-data:
  whiteboard-shared:
